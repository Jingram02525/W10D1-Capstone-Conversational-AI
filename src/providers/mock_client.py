def ask(prompt: str, model: str | None = None, timeout: int = 30) -> str:
    # Trivial, predictable mock for offline demos and fallbacks.
    return "Pomodoro is 25 minutes of focus followed by a 5-minute break."
